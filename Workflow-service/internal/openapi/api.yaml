# TODO Добавить paths, поле parameters
openapi: 3.0.0
info:
  title: Manager distribution system (Workflow)
  description: Workflow's OpenAPI
servers:
  - url: http://localhost:8080
    description: local
paths:
components:
  schemas:
    $ref: "./schemas/index.yaml"
