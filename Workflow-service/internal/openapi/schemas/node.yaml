type: object
required: [id, type]
properties:
  id:
    type: string
  type:
    type: string
    enum:
      - ActionNode
      - PredicateNode
      - ConditionNode
  data:
    x-go-type: "interface{}"
    oneOf:
      - $ref: "./action.yaml"
      - $ref: "./predicate.yaml"
      - $ref: "./conditionGroup.yaml"
  position:
    $ref: "./position.yaml"
