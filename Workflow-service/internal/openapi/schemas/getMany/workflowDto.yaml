type: object
required: [id, name, status, createdAt]
properties:
  id:
    type: string
    description: Id воркфлоу
  name:
    type: string
    description: Имя воркфлоу
  status:
    type: string
    enum:
      - created
      - active
      - paused
      - archived
  createdAt:
    type: string
    description: Дата создания воркфлоу
